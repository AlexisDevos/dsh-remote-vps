[package]
name = "dsh-protocol"
version = "0.3.0"
edition = "2021"
license = "MIT"
description = "Versioned Protobuf/gRPC contract for dsh-remote-vps"

[dependencies]
prost = "0.13.5"
tonic = { version = "0.12.3", features = ["transport"] }

[build-dependencies]
protoc-bin-vendored = "3.1.0"
tonic-build = "0.12.3"
